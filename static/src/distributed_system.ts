class Distributed_System {
        graph: Graph;
        code: string;

        constructor(code: string) {
            this.graph = new Graph() as Graph;
            this.code = code;
        }
    }
